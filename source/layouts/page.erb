<% wrap_layout :layout do %>
  <h1><%= current_page.data.title || "Title T.K." %></h1>
  <%= yield %>
  <% if current_page.data.date %>
    <time><%= Date.parse(current_page.data.date).strftime('%B %e, %Y')  %></time>.
    Questions or comments? <a href="/pages/contact.html">Let me know what you think</a>.
  <% end %>
<% end %>